package WithoutStrategyPattern;

public class PassengerVehicle extends Vehicle{
    /**it has the normal drive capability
     so no need to override the parent's drive method, it will use parent's method only
     **/
}

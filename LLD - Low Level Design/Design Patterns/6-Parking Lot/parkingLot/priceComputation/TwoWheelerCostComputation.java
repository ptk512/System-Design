package parkingLot.priceComputation;

public class TwoWheelerCostComputation extends CostComputation{

    public TwoWheelerCostComputation(PriceStrategy priceStrategy) {
        super(priceStrategy);
    }
}
